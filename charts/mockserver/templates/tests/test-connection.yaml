apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mockserver.fullname" . }}-test-connection"
  labels:
    {{- include "mockserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mockserver.fullname" . }}:{{ .Values.service.port }}/status']
  restartPolicy: Never
